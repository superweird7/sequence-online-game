# Online Board Game

Private online board game for friends. Built with Node.js, Express, and Socket.IO.

## Run locally

```bash
npm install
npm start
```

Open `http://localhost:3000`.

## Deploy

Use any Node.js host that supports WebSockets.

Build command: `npm install`
Start command: `npm start`
